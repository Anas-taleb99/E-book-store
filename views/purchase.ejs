<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>purchase tabel </title>
  <%- include('style') %>
</head>
<body>
  <%- include('./nav') %>
  <table class="table table-striped table-dark">
    <thead>
      <tr>
        <th scope="col">Username</th>
        <th scope="col">Book name</th>
        <th scope="col">Date</th>
        <th scope="col">Author</th>
        <th scope="col">Publisher</th>
        <th scope="col">Price</th>
      </tr>
    </thead>
    <tbody>
      <%for(let i = 0; i < mypurchase.length; i++) {%>
        <tr>
          <td><%=mypurchase[i].user_name%></td>
          <td><%=mypurchase[i].book_name%></td>
          <td><%=mypurchase[i].date%></td>
          <td><%=mypurchase[i].author%></td>
          <td><%=mypurchase[i].publisher ? mypurchase[i].publisher : "unknown"%></td>
          <td><%=mypurchase[i].price ? mypurchase[i].price : "Free"%></td>
        </tr>
      <%}%>
    </tbody>
  </table>
</body>
</html>